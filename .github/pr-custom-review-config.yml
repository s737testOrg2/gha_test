approval_groups:
  - name: HEADS
    condition: 👤.*(\n^[\+|\-].*)|^[\+|\-].*👤
    check_type: pr_diff
    min_approvals: 2
    users:
    teams:
      - s737team
  - name: BAGS
    condition: /👜.*(\n^[\+|\-].*)|^[\+|\-].*👜/
    check_type: pr_diff
    min_approvals: 1
    users:
      - sergioko747
      - sergioko737
      - sergioko757
      - sergejparity
    teams:
  - name: FILES
    condition: .*/runtime/.*lib.rs|.gitlab-ci.yml
    check_type: pr_files
    min_approvals: 2
    users:
    teams:
      - s737team
