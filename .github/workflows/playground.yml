name: Playground

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: pr-custom-review
        run: |
          echo "SET"
          set
          echo "ENV"
          env
